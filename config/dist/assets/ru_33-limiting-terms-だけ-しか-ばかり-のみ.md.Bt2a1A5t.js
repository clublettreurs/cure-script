import{_ as o,a as c,b as d,c as p,d as a,e as t,f as r}from"./chunks/image770.CDQ7qBeD.js";import{_ as s,c as i,a2 as n,o as m}from"./chunks/framework.DvpCT43G.js";const k=JSON.parse('{"title":"33. Ограничительные слова: だけ, しか, ばかり, のみ","description":"","frontmatter":{},"headers":[],"relativePath":"ru/33-limiting-terms-だけ-しか-ばかり-のみ.md","filePath":"ru/33-limiting-terms-だけ-しか-ばかり-のみ.md","lastUpdated":1754029915000}'),l={name:"ru/33-limiting-terms-だけ-しか-ばかり-のみ.md"};function h(_,e,u,f,g,b){return m(),i("div",null,e[0]||(e[0]=[n('<h1 id="_33-ограничительные-слова-だけ-しか-ばかり-のみ" tabindex="-1"><strong>33. Ограничительные слова: だけ, しか, ばかり, のみ</strong> <a class="header-anchor" href="#_33-ограничительные-слова-だけ-しか-ばかり-のみ" aria-label="Permalink to &quot;**33. Ограничительные слова: だけ, しか, ばかり, のみ**&quot;">​</a></h1><p><a href="https://www.youtube.com/watch?v=OoJLexUR_o0&amp;list=PLg9uYxuZf8x_A-vcqqyOFZu06WlhnypWj&amp;index=35&amp;pp=iAQB" target="_blank" rel="noreferrer"><strong>Урок 33: だけ, しか, ばかり, のみ: разбираемся в японских ограничительных словах.</strong></a></p><p>こんにちは。</p><p>Сегодня мы поговорим об ограничительных словах: <code>だけ</code>, <code>しか</code>, <code>ばかり</code> (мы уже рассматривали <code>ばかり</code> в отдельном видео, но сегодня посмотрим, как оно вписывается в эту тему) и <code>のみ</code>. <code>のみ</code> очень простое, и вам оно всё равно не понадобится, но важно понимать его, когда вы его видите.</p><p>Многие находят эти слова запутанными, но это только потому, что их преподают запутанно. Как только вы поймёте, как они на самом деле работают, они покажутся совсем несложными.</p><h2 id="だけ" tabindex="-1">だけ <a class="header-anchor" href="#だけ" aria-label="Permalink to &quot;だけ&quot;">​</a></h2><p>Итак, давайте начнём с самого базового слова, <code>だけ</code>. <code>だけ</code> означает <code>предел</code>. Нам иногда говорят, что оно означает <code>только</code>, и в своей самой базовой форме выражения, это то, что мы бы перевели как «только».</p><p>Однако, при некоторых других его использованиях важно понимать, что на самом деле оно означает <code>предел</code>. Итак, если я говорю <code>千円だけ持っている</code>, я говорю: «У меня есть предел в тысячу иен / Тысяча иен — это предел того, что у меня есть».</p><p><img src="'+o+'" alt=""></p><p><code>だけ</code> — это существительное, и когда мы ставим <code>千円</code> перед ним, мы используем <code>千円</code> как определение к существительному <code>だけ</code>.</p><p><img src="'+c+'" alt=""></p><p>Таким образом, мы говорим, что у меня есть предел в тысячу иен, тысяча иен — это всё, что у меня есть. Это достаточно просто. Мы вернёмся к <code>だけ</code> через мгновение.</p><p>Теперь давайте посмотрим на <code>しか</code>.</p><h2 id="しか" tabindex="-1">しか <a class="header-anchor" href="#しか" aria-label="Permalink to &quot;しか&quot;">​</a></h2><p>Итак, <code>しか</code> действительно сбивает людей с толку, и это потому, что им создаётся впечатление, что оно означает более или менее то же самое, что и <code>だけ</code>. А на самом деле оно означает противоположное <code>だけ</code>.</p><p><code>しか</code> означает <code>больше, чем</code>.</p><p><img src="'+d+'" alt=""></p><p>И если мы это поймём, мы никогда не запутаемся в <code>しか</code>, потому что оно очень простое. Суть в том, что оно используется только в отрицательных предложениях. Итак, у нас всегда есть <code>ない</code> или <code>ありません</code>, когда мы используем <code>しか</code>, поэтому в итоге оно означает <code>не больше, чем</code>.</p><p>И это то, что делает его очень похожим на <code>だけ</code>. Но если мы не осознаём, что оно на самом деле означает <code>больше, чем</code>, мы очень путаемся в том, как оно структурно вписывается в предложение.</p><p>Итак, если мы говорим <code>千円だけ持っている</code>, мы говорим, что тысяча иен — это предел того, что у меня есть. Если мы говорим <code>千円しか持っていない</code>, мы говорим, что у меня нет больше тысячи иен. И, как вы видите, это отрицательное предложение, и акцент делается на отрицании.</p><p>Это очень похоже на то, что мы могли бы сказать по-английски: мы могли бы сказать <code>I only have a thousand yen</code> / <em>(<code>千円だけ持っている</code>)</em> или мы могли бы сказать <code>I don&#39;t have any more than a thousand yen</code> / <em>(<code>千円しか持っていない</code>)</em>. И вы видите разницу между этими двумя, и разница в японском языке точно такая же.</p><p><code>だけ</code> не подразумевает, что тысяча иен — это много или мало; оно ничего не подразумевает, оно просто говорит, что это то, что у меня есть, и это всё, что у меня есть. <code>I don&#39;t have any more than a thousand yen/<em>千円しか持っていない</em></code> делает акцент на том, что этого может быть слишком мало, или что если вы захотите больше, то не получите этого, или что-то в этом роде.</p><p>Оно имеет этот отрицательный акцент, потому что мы делаем весь акцент на том, чего у меня нет, а не на том, что у меня есть. И это, в данном контексте, разница между <code>だけ</code> и <code>しか... ない</code>.</p><p>И <code>しか... ない</code> также может использоваться в таких обстоятельствах, как <code>にげるしかない</code>. <code>にげる/<em>逃げる</em></code> означает <code>убегать</code> или <code>спасаться</code>; если мы говорим <code>にげるしかない</code>, мы говорим: <code>Ничего не остаётся, кроме как бежать / Нет другого выхода, кроме как бежать</code>.</p><p>Итак, точно так же, как <code>千円しかない</code> означает (у меня) нет больше тысячи иен, <code>にげるしかない</code> означает, что нет ничего, что мы можем сделать, кроме как бежать. А теперь давайте вернёмся к некоторым другим использованиям <code>だけ</code>.</p><h2 id="другие-использования-だけ" tabindex="-1">Другие использования だけ <a class="header-anchor" href="#другие-использования-だけ" aria-label="Permalink to &quot;Другие использования だけ&quot;">​</a></h2><p>Одно из самых распространённых — это <code>できるだけ</code>, что означает <code>насколько возможно</code> или <code>если вообще возможно</code>. Теперь, видите, в этот момент, если мы думаем о <code>だけ</code> как о значении <code>только</code>, мы можем начать путаться.</p><p>Это совершенно другой вид <code>だけ</code>? Нет, это то же самое. <code>できるだけ</code> означает <code>до предела возможности</code>.</p><p><code>できるだけ勉強します</code> — <code>Я буду учиться, если смогу</code> или <code>Я буду учиться столько, сколько смогу</code> / <code>до предела возможности я буду учиться</code>.</p><p><img src="'+p+'" alt=""></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>На случай, если кто-то задавался вопросом…</p></div><p>Ещё одно использование, которое вы, безусловно, будете видеть довольно часто, это <code>だけあって</code>. Это <code>あって</code> — это <code>ある</code> — <code>быть</code>. И нам часто говорят, что оно означает что-то вроде <code>не зря</code>.</p><p>Итак, <code>留学しただけあって英語はうまい</code>.</p><p><img src="'+a+'" alt=""></p><p>И это буквально означает: <code>В силу ограничения, связанного с тем, что она училась за границей...</code> (и «потому что» здесь — это て-форма, которая часто подразумевает причину последующего эффекта) <code>... её английский превосходен</code>. Перевод, который нам дают: <code>Не зря она училась за границей, её английский превосходен</code>.</p><p>Но на самом деле здесь говорится: <code>Именно потому, и только потому, что она училась за границей, её английский превосходен</code>. Теперь вы можете подумать, что я придираюсь к мелочам и слишком занудствую по поводу точного значения.</p><p>Но давайте возьмём другой пример. <code>安いだけあってすぐに壊れちゃった</code>. Теперь это означает: <code>В силу ограничения, связанного с его дешевизной, оно быстро сломалось</code>.</p><p>Теперь, не имело бы никакого смысла здесь говорить: <code>Не зря оно было дешёвым, оно быстро сломалось</code>. На самом деле мы говорим: <code>Именно потому, что оно было дешёвым, оно быстро сломалось</code>.</p><p><img src="'+t+'" alt=""></p><p>Это <code>だけ</code> использует ограничительную функцию, чтобы сузить что-то до чего-то точного. Если мы хотим ввести аспект «только», то это работает так: мы говорим: <code>Только благодаря обучению за границей можно так хорошо выучить английский</code>. <code>Только что-то действительно дешёвое сломалось бы так быстро</code>.</p><p>Итак, вот как на самом деле работает функция ограничения, «только», у <code>だけ</code>. Мы используем её, чтобы придать точность утверждению: <code>Именно и только из-за этого последовал результат</code>; <code>だけあって</code> — <code>Оно существует из-за этого факта и ограничено им</code>.</p><p>Теперь давайте введём <code>ばかり</code>.</p><h2 id="ばかり" tabindex="-1">ばかり <a class="header-anchor" href="#ばかり" aria-label="Permalink to &quot;ばかり&quot;">​</a></h2><p><code>ばかり</code>, как мы знаем, также выражает тот же вид ограничений. Оно означает <code>именно такая-то вещь</code>. Итак, давайте сравним его с двумя другими.</p><p>Если мы говорим <code>あのお店はパンだけ売る</code>, мы говорим: <code>Этот магазин продаёт только хлеб</code>. Если мы говорим <code>あのお店はパンしか売らない</code>, мы говорим: <code>Этот магазин не продаёт ничего, кроме хлеба</code>.</p><p>Если мы говорим <code>あのお店はパンばかり売る</code>, мы снова говорим: <code>Этот магазин продаёт только хлеб</code>, но, как мы знаем из урока о <code>ばかり</code>, мы, скорее всего, имеем в виду, что <code>Этот магазин продаёт ужасно много хлеба</code>. Может быть, даже неправда, что он продаёт только хлеб, потому что <code>ばかり</code> может использоваться гиперболически.</p><p>Мы можем сказать <code>東京は外人ばかりだ</code> — <code>В Токио нет ничего, кроме иностранцев</code>, что не означает, так же, как и в английском, что в Токио на самом деле нет японцев. Это означает, что в Токио ужасно много иностранцев. Итак, мы можем расположить эти три на скользящей шкале.</p><p><img src="'+r+'" alt=""></p><p><code>パンしか売らない</code> подразумевает, что продавать только хлеб — это очень мало, этого недостаточно. Возможно, мы хотим что-то, кроме хлеба, но не можем это получить. <code>パンだけ売る</code> не имеет никаких подтекстов. Оно нейтрально.</p><p>Это может означать, что магазин специализируется на хлебе и поэтому очень хорош. Это может означать, что мы ничего не можем получить, если хотим что-то, кроме хлеба. Оно не имеет никакого особого подтекста. Оно просто нейтрально говорит, что магазин продаёт только хлеб.</p><p><code>パンばかり売る</code> подразумевает, что там ужасно много хлеба. Тот факт, что оно ограничено хлебом, по сути, подчёркивает, что хлеба там в изобилии до такой степени, что мы могли бы гиперболически сказать, что там нет ничего, кроме хлеба, хотя с <code>ばかり</code> на самом деле это может быть не так.</p><h2 id="のみ" tabindex="-1">のみ <a class="header-anchor" href="#のみ" aria-label="Permalink to &quot;のみ&quot;">​</a></h2><p>И прежде чем мы закончим, я просто расскажу о <code>のみ</code>. <code>のみ</code> очень простое, потому что всё, что оно означает, это <code>だけ</code> в его простейшем смысле.</p><p>Итак, мы говорим <code>パンだけ売る / パンのみ売る</code>. Они оба означают одно и то же. Они означают <code>продаёт только хлеб</code> без какого-либо особого подтекста.</p><p><code>のみ</code> используется в формальных контекстах, в основном используется в письменной речи, и если вы не пытаетесь использовать очень формальный японский, вам оно на самом деле не понадобится. Причина, по которой важно его знать, заключается в том, что, например, если бы вы собирались войти куда-то, а вывеска сообщала бы вам, что допускаются только члены, это могло бы спасти вас от большого смущения, если бы вы знали, что это <code>のみ</code> означает то же самое, что и <code>だけ</code>.</p><p>И такой вид вывесок — именно там, где они использовали бы <code>のみ</code>, потому что оно, как правило, используется в таких формальных, официальных контекстах.</p>',56)]))}const v=s(l,[["render",h]]);export{k as __pageData,v as default};
